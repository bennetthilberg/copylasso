# CopyLasso v0.2 Release State

This record closes the public CopyLasso 0.2 release line through maintenance
release 0.2.1 without changing either release's immutable tag, application,
disk image, checksum, or authenticated update metadata.

## Current Public Release

- Release: [`v0.2.1`](https://github.com/bennetthilberg/copylasso/releases/tag/v0.2.1)
- Release ID: `367570430`
- Release commit: `813de17c739097217aad55a5a35c04ea3c73d99f`
- Published: `2026-08-09T19:59:13Z`
- Final tag: signed annotated `v0.2.1`, verified by GitHub and peeled to the
  release commit
- Final tag object: `eec528b9daf3026aa1e8c4e10acab41b91e37bc3`
- Release candidate: `v0.2.1-rc.1`, unchanged and pointing directly to the
  same release commit
- Release state: public, non-prerelease, and latest

The final release contains exactly two uploaded assets:

| Asset | Size | GitHub SHA-256 |
| --- | ---: | --- |
| `CopyLasso-0.2.1.dmg` | 3,737,908 bytes | `05180caa3600bcd282246297a9172517136e43e55c6e8fa192b55ba44af4a017` |
| `CopyLasso-0.2.1.dmg.sha256` | 86 bytes | `b9a85f82686dce479cb41247fe9fc025ec8a0d099bbc08028c4239899359b1c9` |

DMG SHA-256: `05180caa3600bcd282246297a9172517136e43e55c6e8fa192b55ba44af4a017`

Checksum asset SHA-256: `b9a85f82686dce479cb41247fe9fc025ec8a0d099bbc08028c4239899359b1c9`

Fresh unauthenticated downloads matched the approved candidate bytes.
GitHub's generated source ZIP and tarball each matched `git archive` at
`v0.2.1`. The private dSYM and verification bundle remain restricted to the
unchanged release-candidate draft.

## Current Public Update Feed

The production appcast remains available at
[`https://updates.copylasso.com/appcast.xml`](https://updates.copylasso.com/appcast.xml).
The custom domain, assigned Pages hostname, and immutable deployment hostname
all served the same authenticated bytes with XML content type, `nosniff`, and
the reviewed five-minute `must-revalidate, no-transform` cache policy.

Appcast SHA-256: `c721b9396682c05082e019bdfa1297bc320f9883aabac2fd20c647f228aa8454`

Production feed deployment: `e768eb55-98d7-4d44-9603-65e3972fd66d`

The feed and enclosure signatures authenticate against the public key compiled
into both updater-enabled public releases.
The exact public `0.2.0 (3)` application updated through the production feed to
byte-identical public `0.2.1 (4)` after separate download and
install-and-relaunch consent. The authenticated high-water build advanced to
`4`, and rollback remains rejected.

## Current Installed Application

The installed public application reports `0.2.1 (4)`, production bundle
identifier `io.github.bennetthilberg.copylasso`, and native `x86_64` plus
`arm64` slices. It is byte-identical to the application inside the public DMG
and passed strict Developer ID, Hardened Runtime, reviewed entitlement,
nested-code, notarization/stapling, Gatekeeper, and read-only two-item DMG
checks. Safari-private downloads of both public assets carried genuine
Safari-origin quarantine.

The maintainer launched those exact installed bytes, completed one shortcut
capture with successful clipboard output, HUD, and enabled success sound, then
confirmed the bounded **CopyLasso Is Up to Date** result against the public
feed. No release byte, tag, asset, or feed value was replaced during closure.

## Shipped v0.2.1 Scope

Public 0.2.1 retains the updater, optional success sound, and unified local OCR
plus QR, Code 128, Data Matrix, PDF417, and Aztec recognition from 0.2.0. It
also ships bounded native Markdown presentation for authenticated update notes,
the focus-preserving macOS interactive selector, concise update labels, clearer
About spacing, shorter privacy copy, and additional release and lifecycle
hardening. It adds no recognition mode, permission category, entitlement,
dependency, content persistence, network path, processing indicator, or LaTeX
recognizer.

## Historical v0.2.0 Public Release

- Release: [`v0.2.0`](https://github.com/bennetthilberg/copylasso/releases/tag/v0.2.0)
- Release ID: `361797888`
- Release commit: `43f1d0c676b08fb24b49fc628213fede90c4ed9d`
- Published: `2026-07-29T14:43:30Z`
- Final tag: signed annotated `v0.2.0`, verified by GitHub and peeled to the
  release commit
- Release candidate: `v0.2.0-rc.1`, unchanged and pointing to the same release
  commit
- Release state: public, non-prerelease, and superseded by v0.2.1

The final release contains exactly two uploaded assets:

| Asset | Size | GitHub SHA-256 |
| --- | ---: | --- |
| `CopyLasso-0.2.0.dmg` | 3,665,931 bytes | `697cb008cf294b32500e2ad84e5777a51fe8b88916856c5a8e9f1ec4eb74ba19` |
| `CopyLasso-0.2.0.dmg.sha256` | 86 bytes | `6dfd44d92f6af1c14d765bc6c827ed3e9a0edd5ffe289c3e74ac6e1dd0c834b0` |

DMG SHA-256: `697cb008cf294b32500e2ad84e5777a51fe8b88916856c5a8e9f1ec4eb74ba19`

Checksum asset SHA-256: `6dfd44d92f6af1c14d765bc6c827ed3e9a0edd5ffe289c3e74ac6e1dd0c834b0`

The public checksum validates the exact public DMG. GitHub's generated source
ZIP and tarball each compare file-for-file with `git archive` at `v0.2.0`.
The private dSYM and verification bundle remain restricted to the unchanged
release-candidate draft.

### Historical v0.2.0 Update Feed

The production appcast remains available at
[`https://updates.copylasso.com/appcast.xml`](https://updates.copylasso.com/appcast.xml).
The G44 readback returned HTTP 200 with the expected XML content type, immutable
v0.2.0 enclosure URL, version ordering, file length, and release notes.

Appcast SHA-256: `a6be6c899e31e5913d5be315f209884100f709bd0e13d7490da8f07c9ed08ace`

The feed signature and enclosure signature authenticate against the public key
compiled into the exact public app. Tampered feed and package fixtures remain
rejected by the release verification suite.

### Historical v0.2.0 Installed Application

The installed `/Applications/CopyLasso.app` is byte-identical to the application
inside the exact public DMG. Its readback reports the production bundle
identifier, version `0.2.0`, build `3`, and both `arm64` and `x86_64` slices.
Launch Services was reduced to one production CopyLasso registration at that
installed path without deleting archived qualification evidence or resetting
unrelated application registrations. A reset scoped only to production
CopyLasso removed its stale duplicate Screen Recording record; System Settings
then showed one enabled, branded CopyLasso entry and left every unrelated
privacy row unchanged.

G43's exact package qualification passed Developer ID, Hardened Runtime,
entitlement, nested-code, notarization, stapling, Gatekeeper, Universal 2, and
two-item read-only DMG checks before publication. During the G44 readback on the
same host, the exact digest-matching bytes reproduced the previously documented
macOS 26 command-line provenance anomaly: one Gatekeeper assessment recognized
the notarized Developer ID application, while later `codesign` and `stapler`
invocations returned local subsystem errors. The immutable bytes, GitHub
digests, mounted/installed byte comparison, authenticated appcast, and prior
strict qualification all remained unchanged; this record does not relabel the
host anomaly as a fresh strict pass.

### Historical v0.2.0 Scope And Issues

Public 0.2.0 ships the user-controlled authenticated updater, the configurable
candidate-17 success sound, and unified local OCR plus QR, Code 128, Data Matrix,
PDF417, and Aztec recognition. Code payloads remain inert plain text and an
eligible code takes precedence over OCR in the same selection.

Issue [#36](https://github.com/bennetthilberg/copylasso/issues/36) closed after public updater verification.

Issue [#38](https://github.com/bennetthilberg/copylasso/issues/38) closed after public code-recognition verification.

Issue [#49](https://github.com/bennetthilberg/copylasso/issues/49) remains open because v0.2.0 contains no LaTeX recognizer.

The G43A release-note presentation fix was merged after v0.2.0 publication and
is not present in immutable public 0.2.0 bytes. It is included in immutable
public v0.2.1 bytes and is dated under that release in the changelog.
