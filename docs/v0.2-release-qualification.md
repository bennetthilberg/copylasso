# CopyLasso v0.2 Source Qualification

**Version/build:** `0.2.0 (3)`
**Execution status:** Complete with explicitly recorded manual gaps
**Public status:** Public release remains `0.1.1`.

This record freezes and qualifies the source intended for CopyLasso v0.2
without creating a release candidate or publishing an updater feed. G42 is a later release gate that may create one immutable private candidate only after
G41 merges to protected `main`.

The exact G42 candidate qualification is recorded in
[`v0.2-release-candidate.md`](v0.2-release-candidate.md). This G41 record
remains the historical source-qualification boundary and does not silently
promote its blocked rows.

## Frozen Feature Set

- One Capture command and configurable shortcut recognize ordinary horizontal
  U.S. English text plus QR, Code 128, Data Matrix, PDF417, and Aztec codes.
  An eligible code result wins over OCR text; otherwise Capture falls back to
  text.
- The user-controlled Sparkle updater checks one fixed authenticated feed.
  Automatic checks default on and can be disabled. Download and installation
  each require separate user consent.
- One original success sound defaults on, can be disabled independently, and
  plays only after a successful clipboard write.
- G39 concluded no-go. Version 0.2 contains no LaTeX recognizer, model, runtime,
  command, dependency, or precedence branch.

## Qualification Boundary

G41 combines fresh automated and signed delta checks with retained historical
evidence. It never upgrades a historical **Blocked** or **Skipped** row to a
pass without a new observation.

| Boundary | G41 evidence |
| --- | --- |
| Automated source and integration | **Pass** — canonical arm64 and x86_64 each passed 349 tests, the offline suite, three repeatability runs, all focused audits, Debug and Release builds, and Universal 2 verification. Hosted arm64, x86_64, and maintained macOS 15 arm64 checks passed at the reviewed pull-request head. |
| Signed current-host UI and accessibility | **Partial** — isolated signed UI batches passed menu, Settings, About, shortcut, updater, unified-result HUD, permission-recovery, focus, appearance, and keyboard checks. A missing Launch at Login accessibility label was reproduced, fixed, and rerun successfully. Physical VoiceOver speech and a second display were unavailable and are not inferred. |
| Performance and private-data lifetime | **Partial** — the clean signed app passed the 60-second idle and zero-leak gates, and the automated 100-result integration cycle passed. Physical 30-text/30-code latency and signed-process 100-cycle memory sampling could not be driven faithfully by the available automation and remain Blocked. |
| macOS 14 behavior | **Partial** — a separate disposable Sonoma 14.6.1 (23G93) guest Safari-downloaded the exact qualified DMG with genuine quarantine, verified its digest, passed Gatekeeper and first-run onboarding, exercised Screen Recording denial and recovery, copied the expected OCR result, observed default-on and disabled sound settings, handled an unavailable update safely, and quit/relaunched normally. VirtualBuddy stopped reliably re-arming selection after the first successful capture, so QR-over-text precedence, audible sound output, and a post-updater capture remain Blocked rather than inferred. |
| Distribution package | **Pass locally** — the exact source commit produced a Developer ID, Apple-accepted, stapled Universal 2 app and signed DMG. Strict signatures, reviewed entitlements, Gatekeeper, checksum, layout, nested code, and dSYM matching passed. |
| Safe update signing | **Pass locally** — the Keychain public key matched the exact app, the local-only feed and enclosure authenticated, and changed-feed, changed-enclosure, and wrong-key controls were rejected. No feed was uploaded or served. |

Content-free detailed results are added after each gate. Raw screenshots,
profiler traces, logs, private paths, signing identities, account values,
credentials, captured pixels, recognized content, clipboard contents, and
update signing material remain outside the repository.

## Current-Host Results

The clean Apple Development-signed Debug application reports `0.2.0 (3)`,
the Debug bundle identity, arm64, Hardened Runtime, the reviewed Debug
entitlements, and no embedded XCTest payload. Its executable and designated
requirement are content-addressed in the ignored G41 evidence record without
publishing certificate or account values.

Signed UI-test-host batches passed:

- dockless launch, menu order, saved and custom shortcut glyphs, Settings and
  About close/reopen behavior, Finder-frontmost Settings presentation, and
  clean quit;
- updater controls, unified code/no-content/ambiguity HUD copy, permission
  recovery, code precedence, and repeated menu-command reuse;
- light and dark appearances plus keyboard traversal; and
- the corrected Launch at Login accessibility label and value in onboarding
  and Settings.

The first accessibility run found that the native Launch at Login toggles
exposed an empty label on the current runtime. A focused test first captured
the missing contract. Both toggles now receive the same explicit label, and
the focused pure and signed UI reruns pass. No other product correction was
required.

The current host had one display attached, so the physical cross-display row
is **Blocked**. Historical Sidecar evidence remains historical. VoiceOver
speech was not physically repeated in G41; direct labels, values, identifiers,
and keyboard traversal pass, but they are not relabeled as a VoiceOver speech
pass.

## Distribution and Update-Signing Result

The qualified application payload was built from source commit
`beb575c71c8d58660a3666f3cc3ebe67f6ca85e6`. Apple accepted separate
notarization submissions for its Developer ID application and signed DMG. Both
tickets stapled and validated. The final checks confirmed production identity,
`0.2.0 (3)`, arm64 and x86_64, Hardened Runtime, secure timestamps, exact
reviewed entitlements, designated requirements, every nested code signature,
Gatekeeper acceptance, the read-only two-item DMG layout, checksum agreement,
and matching application/dSYM UUIDs. The qualified DMG SHA-256 is
`b162b680d2732f575a06da50766bbbb7ac691c95f4aa0e265bda81456f242845`.

Sparkle's Keychain-derived public key byte-matched the public key compiled into
that application. A local-only one-item feed and the exact DMG enclosure
authenticated successfully; a mutated feed, mutated enclosure, and unrelated
key were rejected. The private key was not exported or logged, and no appcast,
artifact, tag, release, or feed was uploaded, served, or published.

## Manual Delta Matrix

The current-host matrix covers menu, Settings, About, the saved shortcut,
automatic and manual update controls, sound enabled and disabled, text and all
five code formats, code-over-text precedence, ambiguity, no-result, Escape,
rapid reuse, permission denial/recovery/revocation, Launch at Login
reconciliation, lifecycle interruption, physical multi-display clamping,
VoiceOver and keyboard traversal, and light/dark/accessibility appearances.

The Sonoma matrix is intentionally smaller: genuine Safari quarantine, clean
drag installation, Gatekeeper, first launch, permission denial and recovery,
one OCR result, QR-over-text precedence, sound on/off, updater controls with
offline failure, and quit/relaunch. A VirtualBuddy input or system failure is
recorded as **Blocked**, not inferred as a product pass.

The first disposable clone reached the preserved Sonoma 14.8.7 baseline's
login screen, but its credential was unavailable. CopyLasso was never
installed or launched in that clone, which was paused without modifying or
booting the baseline.

A separate fresh disposable guest reported macOS 14.6.1 build 23G93. Safari
downloaded the exact G41 DMG with genuine quarantine, and the guest read back
the qualified SHA-256 digest. The mounted image exposed only CopyLasso and the
Applications alias. Drag installation, Apple's first-launch Gatekeeper check,
onboarding, an initial Screen Recording denial, a single CopyLasso permission
row, administrator-authorized enablement, and Quit & Reopen all behaved as
expected. The first permitted capture copied the two expected OCR fixture
lines exactly. Settings showed automatic updates and the success sound enabled
by default; the sound preference could be disabled. A manual update check
failed safely with an unavailable-update message and explicitly left the
installed app unchanged. Normal menu Quit removed the status item, and
launching the installed application restored it.

VirtualBuddy's nested input then stopped reliably arming the CopyLasso
selection overlay: repeated menu invocations closed the menu without a
crosshair or new selection, and the clipboard correctly retained the earlier
OCR result. Consequently QR-over-text precedence, audible enabled/disabled
sound output, and capture-after-update-failure remain **Blocked** in this guest.
They are not promoted from automated or current-host evidence. The temporary
host-only download server was stopped, the fresh guest was paused, and the
preserved 14.8.7 baseline remained stopped and untouched.

## Performance Protocol

- Record 30 ordinary OCR and 30 code capture-to-clipboard samples. Each series
  must have median at most one second and nearest-rank p95 at most two seconds.
- After a 30-second settle, sample CPU, private memory, and RSS once per second
  for 60 seconds. Settled CPU must remain below 1% on the maintainer host.
- Run 100 mixed text/code/success/cancellation cycles, recording private memory
  and RSS every ten cycles. Require bounded plateau behavior, zero reported
  leaks, complete cleanup, and no retained private content.

### Current Result

After a 30-second settle, 60 one-second samples of the clean signed app
reported 0.0% CPU throughout and a constant 16 MiB resident-memory display.
The detailed physical-footprint sampler recorded 61 observations from
16,417,584 to 16,433,968 bytes, a one-page 16,384-byte difference, with a
16,663,320-byte peak. This **passes** the idle CPU requirement and shows no
unbounded idle growth. A separate read-only inspection of that clean signed
process reported zero leaks and zero total leaked bytes.

The production integration suite passed 100 alternating text/code results in
one reusable coordinator, plus separate alternating success/cancellation and
image/string-release coverage. Attempts to automate real signed selection
timing through the current UI harness could not reliably deliver the status
item or live selection events. Consequently the physical 30-text and 30-code
latency series and signed-process ten-cycle memory checkpoints remain
**Blocked**, rather than borrowing historical G24 timing or promoting
service-boundary tests into physical measurements.

## Retained Limitations

The release continues to document ordinary horizontal single-column U.S.
English OCR, single-display selection clamping, protected-content restrictions,
stationary immediate-reuse crosshair delay, lock-during-active-drag recovery,
and the rare privacy-first clipboard clear-then-write failure. Code recognition
is limited to the five listed formats and deliberately refuses to merge
multiple unique multiline payloads. Existing 0.1.x users must manually install
the first updater-enabled release.

No G41 result publishes a feed, tag, release, website, Mac App Store build, or
Homebrew package.
