# CopyLasso 0.2.1 Source Qualification

**Status:** G48 Phase 1 in progress

**Candidate source:** `0.2.1 (4)`

**Public release:** `0.2.0 (3)` remains latest and unchanged

G48 freezes the merged maintenance patch containing the authenticated
release-note presentation correction, release/security hardening, concise
product copy, and focus-preserving native system selector. It adds no feature,
recognition mode, processing indicator, permission, entitlement, dependency,
or network path.

Phase 1 must pass the focused G48 audit, release metadata and package tests,
privacy/security and updater audits, canonical arm64 and x86_64 pipelines,
Universal 2 Release verification, maintained macOS 15 checks, and exact-head
review. The protected workflow remains draft-only and is not dispatched from
the branch.

After a separate merge approval, Phase 2 creates one immutable private
`v0.2.1-rc.N` candidate from exact protected main. Package, authenticated
update, quarantine, capture, sound, HUD, permission recovery, and lifecycle
evidence must identify the same bytes. G49 alone may publish the patch.

The three accepted native-selector residuals remain disclosed: an entire drag
between one-millisecond samples, Control at mouse-up, and a dragged first-use
confirmation followed immediately by Escape. Cross-display releases cancel.
