# CopyLasso v0.2 Publication Runbook

This runbook promotes only the maintainer-approved G42 candidate. It does not
build, sign, notarize, staple, or package CopyLasso. The final release tag
continues to identify the exact source commit from which the approved candidate
was built.

## Frozen Publication Identity

- Repository: `bennetthilberg/copylasso`
- Candidate source: `43f1d0c676b08fb24b49fc628213fede90c4ed9d`
- Candidate tag: `v0.2.0-rc.1`
- Private candidate release ID: `361203156`
- Final tag: `v0.2.0`
- Final annotated-tag message: `CopyLasso 0.2.0`
- Version and build: `0.2.0 (3)`
- Release-note SHA-256:
  `0b40f9524389b684124189ce743109429af97baf124e28bf1d12313eba26d807`
- Public DMG SHA-256:
  `697cb008cf294b32500e2ad84e5777a51fe8b88916856c5a8e9f1ec4eb74ba19`
- Public checksum SHA-256:
  `6dfd44d92f6af1c14d765bc6c827ed3e9a0edd5ffe289c3e74ac6e1dd0c834b0`

The private dSYM and verification bundle remain attached only to the G42
draft. They are never uploaded to the final release or updater feed.

## Phase 1 - Protected Preparation

Phase 1 begins only after the G43 publication-control pull request is merged to
protected `main`. Dispatch `.github/workflows/prepare-publication.yml` from that
exact protected-main commit and approve the existing `release` environment.

The workflow performs the complete canonical quality gate, then:

1. Downloads release `361203156` with authenticated GitHub access.
2. Verifies its draft/prerelease state, target, reviewed notes, four exact asset
   names, sizes, and GitHub SHA-256 records, and independently proves the
   lightweight RC tag still points directly to the frozen source commit.
3. Downloads and independently hashes all four restricted assets.
4. Reconstructs the G42 verification bundle and reruns Developer ID, package,
   notarization, Gatekeeper, version/build, architecture, entitlement, DMG,
   checksum, dSYM, and source-commit checks without rebuilding.
5. Resolves the pinned Sparkle 2.9.4 tools and exposes the production signing
   seed only to the final-appcast generation step.
6. Generates one signed `appcast.xml` whose enclosure is exactly:

   `https://github.com/bennetthilberg/copylasso/releases/download/v0.2.0/CopyLasso-0.2.0.dmg`

7. Confirms `v0.1.1` is still GitHub's latest public release, then creates one
   private, non-prerelease final draft targeting the frozen candidate commit.
   The draft contains only the approved DMG and checksum.
8. Uploads a private workflow handoff containing the feed-only bundle, candidate
   readback, final-draft readback, and content-free manifest.

The workflow accepts no dispatch input. It cannot create a tag, publish a
release, deploy a feed, replace an asset, or receive Developer ID or
notarization credentials.

Download the handoff with authenticated maintainer tooling and rerun
`scripts/audit-v02-publication.sh --handoff <directory> --candidate-dir
<downloaded-candidate-directory> --application <qualified-CopyLasso.app>`.
The candidate directory and application must be the exact outputs reverified by
the G43 package-verification step. The audit verifies the feed and enclosure
signatures with the public key compiled into that application. Stop if any
readback, digest, signature, or state differs.

## Phase 2 - Signed Tag And Publication

Phase 2 is the separately approved public transaction.

### Preflight

Before mutation, confirm:

- protected `main` contains the merged G43 control commit;
- the worktree is clean;
- `v0.2.0-rc.1` still points directly to the frozen candidate commit;
- candidate release `361203156` is still private, draft, prerelease, and has the
  same four restricted assets;
- no `v0.2.0` tag or release exists;
- the private final draft has exactly the two approved public assets and exact
  notes;
- the handoff appcast and enclosure signatures verify through Sparkle using the
  public key compiled into the candidate; and
- `v0.1.1` remains the latest public release.

### Feed Endpoint Preparation

Create a Direct Upload Cloudflare Pages project named `copylasso-updates`.
Associate `updates.copylasso.com` with the Pages project before changing DNS.
The authoritative DNS remains at Spaceship; add only a DNS CNAME for `updates`
pointing to the assigned `pages.dev` hostname. Do not move nameservers or
change the `copylasso.com` apex.

Wait for the custom hostname and TLS certificate to become active. Before the
release is public, `https://updates.copylasso.com/appcast.xml` must remain
absent rather than serving incomplete or unsigned metadata.

The production deployment directory contains exactly:

- `appcast.xml`
- `_headers`

There is no index page, general website, executable code, analytics, redirect,
or content endpoint. The headers require XML content, `nosniff`, and a
five-minute revalidation window with `no-transform`.

### Final Tag

Use the existing dedicated passphrase-protected release-tag SSH key. Create an
annotated signed `v0.2.0` tag with message `CopyLasso 0.2.0` on the frozen
candidate source commit. Verify it locally using a temporary allowed-signers
file, then push only `refs/tags/v0.2.0`.

GitHub readback must show an annotated tag object, a valid verified signature,
and an object that peels to the candidate source commit. The direct
`v0.2.0-rc.1` tag must remain unchanged.

Never move either tag.

### GitHub Release

Read the private final draft by numeric release ID. Publish that exact draft as
non-prerelease and latest. Resolve an ambiguous API result through exact
readback before retrying.

The final release has exactly two maintainer-uploaded assets:

- `CopyLasso-0.2.0.dmg`
- `CopyLasso-0.2.0.dmg.sha256`

Never replace a public asset. GitHub-generated source ZIP and tarball links are
expected and do not count as uploaded assets.

### Authenticated Feed

Only after the GitHub tag, release, and download URL pass readback, deploy the
exact handoff feed directory to the production Cloudflare Pages deployment.
Read `appcast.xml` through both the custom hostname and assigned Pages hostname
and compare the bytes with the private handoff before trusting the deployment.

The signed metadata must contain exactly one `0.2.0 (3)` item, the exact final
GitHub enclosure URL and byte length, the approved inline plain-text notes, and
no external release-notes URL.

## Public Verification

Perform all checks without authenticated download URLs:

1. Confirm the release is public, non-prerelease, latest, and still contains
   only the two approved assets with matching GitHub digests.
2. Confirm the signed annotated final tag and direct RC tag both identify the
   frozen source commit.
3. Download both generated source archives and compare their tracked content
   with the source tree at the final tag.
4. Fetch the updater feed and verify exact bytes, XML structure, feed
   signature, enclosure signature, version ordering, final URL, length, and
   inline notes.
5. In a fresh Safari private session, download the DMG and checksum so the DMG
   receives genuine quarantine metadata.
6. Verify checksum, Developer ID, notarization ticket, Gatekeeper, read-only
   two-item DMG layout, production bundle identifier, `0.2.0 (3)`, reviewed
   entitlements, and both architectures.
7. Reversibly stage the current installation, drag-install the public download,
   launch normally, and run a compact text, QR-precedence, success-sound,
   Settings, update-check, and quit/relaunch smoke.
8. Exercise the reviewed isolated older-updater fixture against the public
   feed. Confirm deferral, download, install/relaunch, exact candidate bytes,
   preserved preferences, and a subsequent up-to-date result.
9. Confirm the first-updater release notes explicitly tell `0.1.x` users to
   install `0.2.0` manually.

Retain content-free records outside the repository under a commit-addressed G43
directory. Do not retain credentials or updater content.

## Failure And Recovery

- Before a final tag exists, a helper may delete only the incomplete private
  final draft that it created in the same invocation.
- After the signed tag is pushed, do not delete or move it. Read back ambiguous
  GitHub state and resume only against the same tag, release ID, and bytes.
- After the release is public, do not delete it, replace assets, edit immutable
  evidence, or publish a corrected appcast automatically. Stop and obtain a
  separately reviewed remediation decision.
- If feed hosting fails after release publication, leave the immutable GitHub
  release intact and keep the feed absent until the exact signed metadata can be
  served.
- A source, application, package, note, entitlement, dependency, or artifact
  change invalidates the candidate and requires a new release candidate.

## G44 Handoff

G43 records live publication evidence only in ignored `roadmap.md` and external
content-free records. G44 separately dates the changelog from GitHub's actual
publication timestamp, updates released-state documentation, closes only
shipped and publicly verified issues, verifies the exact public local
installation, and reconciles protected `main`.

## G49 v0.2.1 Patch Publication

G49 reuses the established boundary for the separately approved maintenance
candidate. Never rebuild the G48 candidate. The frozen identities are:

- candidate commit: `813de17c739097217aad55a5a35c04ea3c73d99f`;
- private release: `367523470`;
- direct candidate tag: `v0.2.1-rc.1`;
- final annotated signed tag: `v0.2.1` with message `CopyLasso 0.2.1`;
- final release name: `CopyLasso 0.2.1`;
- DMG SHA-256: `05180caa3600bcd282246297a9172517136e43e55c6e8fa192b55ba44af4a017`.

The byte-exact notes published with v0.2.1 are retained as the immutable
verification fixture `scripts/fixtures/v0.2.1-published-release-notes.md`.
Reader-facing notes may correct explanatory text, but historical publication
verification must continue to use that pinned fixture and its reviewed digest.

### Phase 1 - Protected Preparation

Merge the green G49 publication-control pull request to protected `main`, then
dispatch only the input-free `copylasso_prepare_publication` event. Approve the
existing `release` environment once. The workflow reruns canonical CI,
downloads and reverifies the four private G48 assets, verifies the restricted
candidate appcast, generates a final-URL authenticated appcast, prepares the
two-file Cloudflare Pages bundle, and creates one private non-prerelease final
draft containing only `CopyLasso-0.2.1.dmg` and its checksum.

Read back the restricted handoff and prove its control commit, candidate
identity, final draft ID, DMG digest, appcast digest, signatures, and exact
file set. The workflow cannot publish, deploy, create a final tag, replace an
asset, or rebuild the candidate.

### Phase 2 - Signed Tag And Publication

Before mutation, reconfirm public `v0.2.0` remains latest and that neither a
`v0.2.1` tag nor release exists. Verify the private final draft, then use the
dedicated release-tag signing identity to create and locally verify the
annotated `v0.2.1` tag on the exact candidate commit. Push only that tag and
require GitHub's verified signed-tag readback before publishing the prepared
draft by release ID as public, non-prerelease, and latest.

Deploy the exact feed handoff to the existing Cloudflare Pages project only
after the immutable GitHub enclosure URL is publicly downloadable. Confirm the
custom domain, assigned Pages hostname, and immutable deployment hostname serve
byte-identical signed metadata with the reviewed XML, cache, and `nosniff`
headers. The authoritative registrar remains Spaceship; G49 changes no apex
record or nameserver.

Never move either tag. Never replace a public asset. Never regenerate the
appcast after publication. Ambiguous API or deployment responses require exact
readback before any retry, and any mismatch stops for a remediation decision.

### Public Verification And Release-State Closure

Verify unauthenticated release state, both asset digests, generated source
archives, checksum, Developer ID signature, notarization/stapling, Gatekeeper,
package layout, metadata, architectures, entitlements, appcast and enclosure
signatures, ordering above build `3`, and a genuine Safari-quarantined install.
Exercise the public updater once from exact public `0.2.0 (3)` to byte-identical
`0.2.1 (4)`. Only after those checks pass may the separate release-state
closure PR date the changelog and describe v0.2.1 as public.

## G50 v0.2.2 Security-Hotfix Publication

G50 publishes only the separately approved Sparkle-security candidate. Its
frozen identities are:

- candidate commit: `81016fe43ee617b5f251564b03904137a4447266`;
- private release: `367632598`;
- direct candidate tag: `v0.2.2-rc.1`;
- final annotated signed tag: `v0.2.2` with message `CopyLasso 0.2.2`;
- final release name: `CopyLasso 0.2.2`;
- DMG SHA-256: `9ac432f956418dd37e04de014867a7fc20d1daeecc80f6fe1db1e9c53b19de2a`.

The exact approved notes are retained at
`scripts/fixtures/v0.2.2-published-release-notes.md` with SHA-256
`df42f13d9ba08fba153b3d7d7d52f828cf9874ea52eec930f249ac7566115af7`.
The immutable v0.2.1 notes and publication controls remain historical and must
continue to verify independently.

### Protected Preparation

After the green G50 publication-control PR is merged, dispatch only the
input-free `copylasso_prepare_v022_publication` event from protected `main`.
The workflow reruns canonical CI, downloads and reverifies all four private
candidate assets, validates Sparkle 2.9.5 and the full-package-only restricted
appcast, generates a final-URL authenticated appcast, prepares the two-file
feed bundle, and creates a private final draft containing only
`CopyLasso-0.2.2.dmg` and its checksum.

Read back the restricted handoff and verify its exact control commit,
candidate tag and release, four source digests, final draft ID, DMG digest,
appcast digest, signatures, and file set. Preparation cannot publish, deploy,
tag, replace assets, or rebuild the candidate.

### Immutable Publication Transaction

Before mutation, reconfirm that public `v0.2.1` remains latest and neither a
`v0.2.2` tag nor release exists. Create and locally verify the annotated signed
`v0.2.2` tag on the exact candidate commit, push only that ref, and require
GitHub's verified signed-tag readback. Publish the prepared draft by release ID
as public, non-prerelease, and latest. Only after its immutable enclosure URL
and both public assets pass unauthenticated readback may the exact signed feed
handoff be deployed to the existing Cloudflare Pages project.

Confirm the custom domain, Pages hostname, and immutable deployment hostname
serve byte-identical appcast bytes with the reviewed cache, XML, and `nosniff`
headers. Never move either tag, replace a public asset, regenerate the appcast,
or rebuild the candidate. Ambiguous responses require exact readback before
continuing; a mismatch stops for remediation.

### Public Verification And Release-State Closure

Verify the public release, two uploaded assets, source archives, checksum,
signatures, notarization, Gatekeeper, package layout, architectures,
entitlements, Sparkle 2.9.5, appcast signatures, build ordering, and a genuine
Safari-quarantined download. Exercise the public updater from exact public
`v0.2.1 (4)` to byte-identical `v0.2.2 (5)`, including separate download and
install consent plus retained preferences, then complete QR and OCR capture
smokes. Only after those checks pass may a separate closure PR date the
changelog and describe v0.2.2 as public.
